 pipeline{
    agent any
tools {
  maven 'maven'
}
    stages {
       

       stage('GetCode') {
            steps{
                git 'https://github.com/bio170/DevOps.git'
            }
         }        
       stage('Build') {
            steps {
                sh 'hello world'
            }
         }
        stage('SonarQube analysis') {
        steps {
        withCredentials([string(credentialsId: 'Sonarqube-1test', variable: 'sonarqube')]) {
        sh "mvn sonar:sonar"
    
          } 

        }
        }

       
    }
}

